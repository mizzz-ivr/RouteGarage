# 作業ログ（2026-07-22 / Issue #91）

## Summary

- PR #90マージ後に到着したCodexレビュー2件を、独立Issue #91・独立branchで修正。
- 交通情報の上流データ由来と調達経路を別軸に分離。
- 提供元最終更新時刻がない情報を最新扱いしない要件を追加。
- PR #92の再レビューで見つかった、状態判定優先順位と事業者一次観測の分類不足を追加修正。
- Source of TruthをIssue #91進行中へ更新。

## Current Issue / PR

- Issue: https://github.com/mizzz-dev/RouteGarage/issues/91
- Current PR: https://github.com/mizzz-dev/RouteGarage/pull/92
- Related Issue: https://github.com/mizzz-dev/RouteGarage/issues/89
- Related PR: https://github.com/mizzz-dev/RouteGarage/pull/90

## Initial Review Findings

### P1: 更新時刻がない情報を最新扱いしない

- RouteGarage受信時刻だけでは上流情報の鮮度を証明できない。
- 提供元最終更新時刻を取得できない情報は最新扱いを禁止する。
- RouteGarage受信時刻を上流更新時刻の代替にしない。

### P2: 契約経路と情報の由来を分離する

- 契約プロバイダーがユーザー投稿・推定情報を再販する可能性がある。
- 上流由来と調達経路を別軸で管理する。
- 契約の有無を正確性や検証済み状態の根拠にしない。

## Re-review Findings

### P1: 更新時刻欠落時の判定優先順位

- 未検証、遅延、提供停止が同時成立し、実装者が安全性の低い状態を選択できる問題を確認。
- 状態を `表示可否`、`鮮度状態`、`検証状態` の3軸へ分離。
- 表示可否を最初に判定し、`提供停止` を他の状態より優先する。
- 安全な表示可否を判断できない場合は、明示的な制限表示ルールがない限り `提供停止` とする。

### P2: 事業者が作成した一次情報の分類不足

- 民間事業者が自社センサー・設備・現地要員から直接作成した未加工情報を分類できない問題を確認。
- 上流由来に `事業者一次観測` を追加。
- 加工・集計済みなら `派生・推定`、利用者投稿の再提供なら `ユーザー投稿` とする境界を定義。

## Completed Tasks

- Issue #91を作成。
- main基点のbranch `docs/issue-91-traffic-source-freshness-fix` を作成。
- `docs/policies/traffic-and-orbis-information-policy.md` を修正。
- `docs/current-status.md` をIssue #91進行中へ更新。
- `docs/active-issues.md` をIssue #91 Activeへ更新。
- Issue #91の作業ログ、AIプロンプトログ、handoffを保存。
- PR #92を作成。
- PR #90のレビューコメントへPR #92を案内。
- PR #92のCodex再レビュー指摘2件を方針文書へ反映。

## Technical Decisions

- 調達経路と上流由来を分離する。
- 上流由来は `公式発表`、`事業者一次観測`、`ユーザー投稿`、`派生・推定`、`不明` で排他的に管理する。
- 状態を `表示可否`、`鮮度状態`、`検証状態` の3軸で管理する。
- `提供停止` > `制限表示` > `表示可` の優先順位とする。
- RouteGarage受信時刻は提供元更新時刻の代替にしない。
- 提供元更新時刻がない情報は最新扱いしない。
- 技術スタック、データ提供元、鮮度閾値は確定しない。

## Rejected Alternatives

- PR #90のマージ済みbranchへ修正を積み続ける案: mainへ取り込まれたか判別しにくくなるため不採用。
- mainへ直接コミットする案: Issue/PRレビューを経ないため不採用。
- 契約プロバイダー情報を一律で公式扱いする案: 上流由来を誤認するため不採用。
- 受信時刻を上流更新時刻として表示する案: 長期キャッシュを最新と誤認するため不採用。
- 未検証、遅延、提供停止を単一の排他的状態として扱う案: 条件重複時に安全性の低い状態を選択できるため不採用。
- 事業者の一次観測を公式発表または派生情報へ含める案: 作成主体と加工有無の区別が失われるため不採用。

## Risks

- 修正前の要件が後続設計で参照されること。
- 契約経路が情報品質の保証と誤解されること。
- 更新時刻不明データが最新表示されること。
- 状態軸が実装時に再び単一値へ統合されること。
- 制限表示の承認条件が未定義のまま実装されること。
- 法務・運用レビューが未完了であること。

## Remaining Tasks

- 修正後のPR #92をCodex再レビューする。
- 未解決レビューコメントがないことを確認する。
- 人間レビューを受ける。
- マージ後にIssue #91を完了する。
- branch cleanupを確認する。

## Suggested Next Actions

1. PR #92の再レビューを実行する。
2. 人間レビュー後にマージする。
3. 法務・運用レビューIssueへ進む。

## AI Prompts Used

- ユーザーの「次のタスク進めて」指示。
- `docs/ai-protocol/PROMPT.txt`。
- PR #90のCodexレビュー2件。
- PR #92のCodex再レビュー2件。

## Handoff

- 実装コードなし。
- 技術選定なし。
- 法的助言ではなく要件の安全側修正。
- AIレビューは人間レビューの代替にしない。
