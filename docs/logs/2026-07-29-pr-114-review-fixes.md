# PR #114 レビュー修正ログ（2026-07-29）

## 対象

- PR: https://github.com/mizzz-ivr/RouteGarage/pull/114
- Issue: https://github.com/mizzz-ivr/RouteGarage/issues/113

## Review Findings

Codexレビューで指摘を合計12件受領した。

### P1: 10件

1. 表示可否・鮮度・検証・権利を単一コードへ統合しない
2. 取得失敗時の旧版・キャッシュ再表示に全条件を課す
3. 提供元更新時点、情報限界、運転判断注意、停車操作注意を固定表示する
4. 共有画像等に生活拠点ぼかしと共有前プレビューを必須化する
5. 走行中も固定必須表示を省略せず、維持不能時は情報本体を非表示にする
6. Current Statusへ検証軸と`STOPPED`優先を同期する
7. Current Statusへ全固定安全表示を同期する
8. handoffへキャッシュ再表示の7条件を同期する
9. handoffへ走行中の全固定必須表示を同期する
10. handoffへ生活拠点ぼかしと共有前プレビューを同期する

### P2: 2件

11. 固定領域で`参考情報`であることを明示する
12. 走行状態不明時に`安全のため操作を制限中`と固定表示する

## 修正内容

### 独立状態軸

- 表示可否
- 鮮度状態
- 検証状態
- 権利状態

利用者向け状態は4軸から導出する。

`STOPPED` > `LIMITED` > `DISPLAYABLE`

権利失効、期限切れ、競合、検証失敗、契約・規約判断不能、安全判断不能時は情報本体を非表示にする。

### キャッシュ再表示

旧版・キャッシュ表示は次の7条件すべての充足時だけに限定した。

1. 規約・契約上の保存・再表示許可
2. 提供元更新時点を取得済み
3. 事前承認済み有効期限内
4. 契約停止、規約変更、撤回、無効化、改ざん・完全性懸念なし
5. 上流由来、調達経路、検証状態を追跡でき、競合なし
6. 取得失敗中、キャッシュ、提供元更新時点を固定表示可能
7. 制限表示ルール、承認者、承認記録あり

1条件でも不足する場合は`STOPPED`。

### 固定安全表示

情報本体表示中は、走行状態にかかわらず次を固定表示する。

- 情報源・提供元
- `参考情報`
- 静的・月次更新情報
- 対象年月・提供元更新時点
- 鮮度・検証状態
- 制限・キャッシュ表示
- 欠落・遅延・誤差・実際との差異の可能性
- 運転判断の唯一根拠にしない旨
- 詳細操作は安全な場所に停車してから行う旨

維持できない場合は情報本体を非表示にする。

### 状態不明時

走行状態不明時は走行中と同等の安全側制御とし、次を固定表示する。

> 走行状態を確認できないため、安全のため操作を制限中です。

無効化した操作の理由を文字と支援技術で説明する。

### 生活拠点プライバシー

共有前に次を必須化した。

- 自宅・職場・開始・終了・反復訪問地点周辺の機械的ぼかし
- 道路形状、表示中心、履歴からの再推定リスク確認
- 共有前プレビュー
- ぼかし解除を公開初期値にしない
- 再推定リスクが残る旨の表示

ぼかし方式未確定の間、位置・走行履歴を含む共有出力はNo-Go。

## Source of Truth同期

次を`docs/current-status.md`、`docs/active-issues.md`、handoffへ同期した。

- 独立4軸と`STOPPED`優先
- 検証未完了・競合・検証失敗の通常表示禁止
- `参考情報`を含む固定必須表示
- キャッシュ再表示の7条件
- 走行中の固定必須表示と維持不能時の非表示
- 状態不明時の操作制限理由
- 生活拠点ぼかしと共有前プレビュー

## 主な修正commit

- `cefaf2bff7a05ba17eb6e4f32c3a53cf7b714332`
- `eb4bd83a0d480c5ea459b8501f358d6bec1fa303`
- `5223a720c536977337823080283d8dc271d2b7c6`
- `fc51825b0ac4d58522b98a14322af94d6e635a49`
- `645b4245d98a91029997f7ce86bf79d1a200bf63`
- `59a0821abd8329b89b195a07cb5d4449088307fc`
- `4e4ed7ce56f5ff9c36bbbc977528fa31d01ddd5c`
- `eb54659db9142d520b6966dff47e13354f4b31c4`

## Review Thread Handling

- P1 10件: 修正・返信・thread解決済み
- P2 2件: 修正・返信・thread解決済み
- 未解決thread 0件を確認後、最新headへ再レビューする

## Impact

- docsのみ
- UI実装なし
- 実データ取得・変換・公開なし
- provider採用・契約・APIキー取得なし
- 外部問い合わせなし

## Remaining Gate

- Codex最終再レビュー
- 人間・法務・運用・安全・プライバシー・アクセシビリティレビュー
- 未解決レビュー0件の確認
- 問題がなければマージ
